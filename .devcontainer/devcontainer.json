{
    "name": "aecert_hexapod ros2 rolling",
    "dockerFile": "Dockerfile",
    "runArgs": [
        "--privileged",
        "--network=host"
    ],
    "workspaceMount": "source=${localWorkspaceFolder},target=/home/vscode/ros2_ws/src/${localWorkspaceFolderBasename},type=bind",
    "workspaceFolder": "/home/vscode/ros2_ws/src/${localWorkspaceFolderBasename}",
    "mounts": [
        "source=${localEnv:HOME}${localEnv:USERPROFILE}/.bash_history,target=/home/vscode/.bash_history,type=bind",
        "source=${localWorkspaceFolder}/.devcontainer/.bash_aliases,target=/home/vscode/.bash_aliases,type=bind"
    ]
}